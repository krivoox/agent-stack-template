/**
 * @deprecated Import from `@/lib/tenancy` instead.
 * Re-export kept so existing feature imports keep compiling during the move.
 */
export {
  requireMembership,
  type MembershipContext,
} from "@/lib/tenancy";
